@startuml View_List_Feedback_Report_Detail_Class_Diagram
title View List Feedback Report Detail - Class Diagram
skinparam backgroundColor white
skinparam monochrome true
skinparam defaultFontColor black
skinparam ArrowColor black
skinparam classBorderColor black
skinparam classFontColor black
skinparam classBackgroundColor white
skinparam stereotypeFontColor black
skinparam linetype polyline
skinparam classAttributeIconSize 0
hide circle

skinparam classHeaderBackgroundColor white
skinparam classHeaderFontColor black

class AdminReportInboxPage {
    - selectedReportId: string | null
    - selectedReport: AdminReviewReport | null
    - detailLoading: boolean
    --
    + AdminReportInboxPage(): JSX.Element
    + selectReport(reportId: string): void
    + loadDetail(): Promise<void>
}

class getReportById {
    --
    + getReportById(reportId: string): Promise<AdminReviewReport>
}

class ApiClient {
    - client: AxiosInstance
    - refreshPromise: Promise<void> | null
    - sessionReconcilePromise: Promise<void> | null
    - isRedirectingToLogin: boolean
    - lastLoginTime: number
    --
    + constructor()
    + get<T>(url: string, config?: AxiosRequestConfig): Promise<T>
    + refreshAccessTokenSingleFlight(): Promise<void>
    - setupInterceptors()
    - isRefreshEndpoint(url?: string): boolean
    - isPublicAuthEndpoint(url?: string): boolean
    - getAuthErrorCode(error: unknown): string | null
    - shouldAttemptSessionReconcile(error: unknown): boolean
    - reconcileSessionSingleFlight(): Promise<void>
    - handleAuthFailure(suppressRedirect: boolean = false)
    ...
}

class ReportController {
    - reportService: ReportService
    --
    + findOne(id: string): Promise<ReportEntity>
    + create(@Body()
}

class ReportService {
    - reportRepo: Repository<ReportEntity>
    - reviewRepo: Repository<ReviewEntity>
    --
    + findOne(reportId: string): Promise<ReportEntity>
    - toBoundedPositiveInt(value: number, fallback: number, max: number): number
    + create(reporterId: string, dto: CreateReportDto)
    + findPending(page: number = 1, limit: number = 20)
    + resolve(reportId: string, dto: ResolveReportDto, adminId: string)
}

class AdminReviewReport {
    + id: string
    + reason: ReportReason
    + description?: string
    + status: ReviewReportStatus
    + adminNote?: string | null
    + createdAt: string
    + resolvedAt?: string | null
    + reporter?: ReviewReportActor | null
    + resolver?: ReviewReportActor | null
    + review: ReviewReportReviewSummary
    --
}

class ReportEntity {
    + id: string
    + reporterId: string
    + reviewId: string
    + resolvedBy?: string | null
    + reason: ReportReason
    + description?: string
    + status: ReportStatus
    + adminNote?: string | null
    + createdAt: Date
    + resolvedAt?: Date | null
    ...
    --
}

class ReviewEntity {
    + id: string
    + reviewerId: string
    + comment: string
    + rating: number
    + deletedAt?: Date | null
    + projectId: string
    + targetUserId: string
    + weight: number
    + createdAt: Date
    + updatedAt: Date
    ...
    --
}

class UserEntity {
    + id: string
    + email: string
    + fullName: string
    + role: UserRole
    + passwordHash: string
    + phoneNumber: string
    + timeZone: string
    + isVerified: boolean
    + resetPasswordOtp: string
    + resetPasswordOtpExpires: Date
    ...
    --
    + badge(): BadgeType
    + warningLevel(): 'NONE' | 'LOW' | 'MEDIUM' | 'HIGH' | 'CRITICAL' | 'SEVERE'
}

AdminReportInboxPage ..> getReportById
AdminReportInboxPage ..> AdminReviewReport

getReportById ..> ApiClient
ApiClient ..> ReportController
ReportController --> ReportService

ReportService ..> ReportEntity
ReportService ..> ReviewEntity
ReportService ..> UserEntity
ReportService ..> AdminReviewReport

@enduml
