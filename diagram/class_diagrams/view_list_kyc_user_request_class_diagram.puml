@startuml View_List_KYC_User_Request_Class_Diagram
title View List KYC User Requests - Class Diagram
skinparam backgroundColor white
skinparam monochrome true
skinparam defaultFontColor black
skinparam ArrowColor black
skinparam classBorderColor black
skinparam classFontColor black
skinparam classBackgroundColor white
skinparam stereotypeFontColor black
skinparam linetype polyline
skinparam classAttributeIconSize 0
hide circle

skinparam classHeaderBackgroundColor white
skinparam classHeaderFontColor black

class ApiClient {
    + get(url: string): Promise<any>
}

class AdminKYCPage {
    - loading: boolean
    - filters: any
    - page: number
    - totalPages: number
    + fetchKycs(): Promise<void>
    + setFilters(filters: any): void
}

class JwtAuthGuard {
    + canActivate(context: any): boolean
}

class RolesGuard {
    + canActivate(context: any): boolean
}

class KycController {
    - kycService: KycService
    + getAllKyc(status: KycStatus, page: number, limit: number): any
}

class KycService {
    - kycRepo: Repository
    + getAllKyc(status: KycStatus, page: number, limit: number): any
}

class Repository {
    + createQueryBuilder(alias: string): any
}

class KycVerificationEntity {
    + id: string
    + userId: string
    + status: KycStatus
    + createdAt: Date
    + reviewedAt: Date
}

enum KycStatus {
    PENDING
    APPROVED
    REJECTED
    EXPIRED
}

AdminKYCPage ..> ApiClient
ApiClient ..> KycController
KycController ..> JwtAuthGuard
KycController ..> RolesGuard
KycController --> KycService
KycService --> Repository
KycService ..> KycVerificationEntity
KycVerificationEntity -- KycStatus

@enduml
